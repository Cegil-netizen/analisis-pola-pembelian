import streamlit as st
import pandas as pd

st.title("Analisis Pola Pembelian (FP-Growth)")

uploaded_file = st.file_uploader("Upload file CSV", type=["csv"])

if uploaded_file:
    df = pd.read_csv(uploaded_file)
    st.write("Data:", df.head())
